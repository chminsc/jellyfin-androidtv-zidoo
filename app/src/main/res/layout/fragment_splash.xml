<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent">

    <ImageView
        android:id="@+id/fragment_startup_logo"
        android:layout_width="400dp"
        android:layout_height="wrap_content"
        android:adjustViewBounds="true"
        android:src="@drawable/app_logo"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintVertical_bias="0.501" />

    <ImageView
        android:layout_width="wrap_content"
        android:layout_height="30dp"
        android:adjustViewBounds="true"
        android:src="@drawable/zidoologo"
        android:layout_marginTop="-15dp"
        app:layout_constraintTop_toBottomOf="@id/fragment_startup_logo"
        app:layout_constraintEnd_toEndOf="@id/fragment_startup_logo"
        />

    <ProgressBar
        android:id="@+id/fragment_startup_progress"
        style="@style/Widget.AppCompat.ProgressBar.Horizontal"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_marginTop="20dp"
        android:indeterminate="true"
        app:layout_constraintEnd_toEndOf="@+id/fragment_startup_logo"
        app:layout_constraintStart_toStartOf="@+id/fragment_startup_logo"
        app:layout_constraintTop_toBottomOf="@+id/fragment_startup_logo" />
</androidx.constraintlayout.widget.ConstraintLayout>
